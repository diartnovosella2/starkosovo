Modular Child Theme 2.0 - boilerplate
---------------------------------------------

## Installation

`yarn install`
or
`npm install`

Run WebPack:
`npm start` - for watch mode
`npm build` - for production